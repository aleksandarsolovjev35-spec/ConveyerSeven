# Зависимости для запуска автотестов.
# Рабочие зависимости линии берутся из requirements.txt — тесты используют
# реальные классы проекта (ProductionCycle, правила, загрузчики), поэтому
# numpy/opencv/pyserial нужны в окружении; ultralytics и pywebview тестам
# не требуются (тяжёлые модели подменяются фейками).
-r requirements.txt

pytest>=8,<9
